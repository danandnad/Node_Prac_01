// 매개변수가 있는 함수 표현식을 화살표 함수로 표현하기  

// 함수 표현식
// let sum = function (a, b) {
//   return a + b;
// }
// console.log(sum(100, 200));

// 화살표 함수
let sum = (a, b) => { return a + b; };
console.log(sum(100, 200));



//함수 선언할때 어떤 값이 필요하다고 표시해놓은 변수를 "매개변수"라고 함
//함수를 실행할때 넘겨주는 변수값을 "인수"라고 함
